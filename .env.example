# Copy this file to `.env` and fill in your real secret values.
# DO NOT commit your real `.env` into source control.

OPENAI_API_KEY="your-openai-api-key-here"
TAVILY_API_KEY="your-tavily-api-key-here"
TAVILY_API_URL="https://api.tavily.com"

LANGFUSE_PUBLIC_KEY="pk-lf-..."
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_HOST="https://cloud.langfuse.com"

SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="your-smtp-user"
SMTP_PASS="your-smtp-password"

VECTOR_DB_DIR=".chroma"
APP_SECRET="change-me"
CONFIDENCE_THRESHOLD=0.65
LOG_FILE="logs.jsonl"
# Example environment variables for Multi-Agent-Email
# COPY this file to `.env` and fill in real secrets (do NOT commit `.env`).

# OpenAI (LLM) API key
OPENAI_API_KEY=

# External search API (optional)
TAVILY_API_KEY=
TAVILY_API_URL=https://api.tavily.com/search

# Vector DB directory (local) used by Retriever/Chroma
VECTOR_DB_DIR=.chroma

# Langfuse (observability) — optional
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# SMTP (email sending) configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Application settings
APP_SECRET=change-me
CONFIDENCE_THRESHOLD=0.65
LOG_FILE=logs.jsonl

# Optional: enable debug / local flags
# DEBUG=true
